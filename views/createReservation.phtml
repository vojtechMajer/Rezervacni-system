<div class="spacer"></div>
<div class="reservation_table">
    <div class="nav">
        <button id="date-prev">
            < </button>
                <input type="date" id="date">
                <button id="date-next"> > </button>
    </div>
    <table id="reservation-table">
    </table>
    <button id="confirm">Confirm Selection</button>
</div>

<form method="post" id="order-form">

    <div id="reservations">
    </div>
    <?php

    if ($user["id_user_type"] == 1) {
        echo '<select name="reservation-type" id="reservation-type">';

        foreach ($reservationTypes as $index => $reservationType) {
            echo "<option value = " . $reservationType->id . ">" . $reservationType->name . "</option>";
        }
    }

    ?>
    </select>

    <input type="submit" value="Submit Order" id="reservation-submit" enctype="multipart/form-data">

</form>
<script src="js/reservations.js"></script>